# AI Provider Configuration
# Options: anthropic, openai, fallback
AI_PROVIDER=fallback

# API Keys (optional - only needed if using AI providers)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Base URL for the application
BASE_URL=http://localhost:3000

# Node Environment
NODE_ENV=development